<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Light Mode) -->
    <style name="Base.Theme.MarsApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- Example: <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Main Theme Reference -->
    <style name="Theme.MarsApp" parent="Base.Theme.MarsApp" />
</resources>
